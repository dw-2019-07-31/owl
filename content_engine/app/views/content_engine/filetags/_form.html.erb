<%= form_with(model: filetag, local: true) do |form| %>
  <% if filetag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(filetag.errors.count, "error") %> prohibited this filetag from being saved:</h2>

      <ul>
      <% filetag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :filename %>
    <%= form.text_field :filename, id: :filetag_filename %>
  </div>

  <div class="field">
    <%= form.label :tag %>
    <%= form.text_field :tag, id: :filetag_tag %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
