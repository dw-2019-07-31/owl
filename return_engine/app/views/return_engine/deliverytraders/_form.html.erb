<%= form_with(model: deliverytrader, local: true) do |form| %>
  <% if deliverytrader.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(deliverytrader.errors.count, "error") %> prohibited this deliverytrader from being saved:</h2>

      <ul>
      <% deliverytrader.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :deliverytrader_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
