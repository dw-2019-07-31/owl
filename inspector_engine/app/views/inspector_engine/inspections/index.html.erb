<p id="notice"><%= notice %></p>

<h1>Inspections</h1>

<table>
  <thead>
    <tr>
      <th>Code</th>
      <th>Houhou</th>
      <th>Houhou eng</th>
      <th>Basho</th>
      <th>Basho eng</th>
      <th>Cost</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @inspections.each do |inspection| %>
      <tr>
        <td><%= inspection.code %></td>
        <td><%= inspection.houhou %></td>
        <td><%= inspection.houhou_eng %></td>
        <td><%= inspection.basho %></td>
        <td><%= inspection.basho_eng %></td>
        <td><%= inspection.cost %></td>
        <td><%= link_to 'Show', inspection %></td>
        <td><%= link_to 'Edit', edit_inspection_path(inspection) %></td>
        <td><%= link_to 'Destroy', inspection, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Inspection', new_inspection_path %>
